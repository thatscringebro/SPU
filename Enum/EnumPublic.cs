namespace SPU.Enum
{
    public enum  Status
    {
        Incomplet,
        Accepté,
        Refusé
    }
    public enum Civilite
    {
        M,
        Mme
    }
    public enum TypeEmployeur
    {
        CISSS,
        CIUSSS
    }
}
